[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "incus_track_les"
version = "0.1.0"
description = "Tracking and analysis tools for INCUS LES simulations"
dependencies = [] 

[tool.setuptools.packages.find]
include = ["incus_track_les*"]  # Force it to only package this specific folder